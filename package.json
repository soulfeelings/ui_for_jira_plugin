{
  "name": "jira-character-customization-plugin",
  "version": "1.1.0",
  "main": "index.js",
  "repository": {
    "type": "git"
  },
  "private": true,
  "author": "Sergei Prilepko",
  "license": "Apache-2.0",
  "scripts": {
    "format": "npx prettier --write ./static ./src ./README.md",
    "watch:ui": "npm start --workspace=static/ui_client",
    "build:ui": "npm run build --workspace=static/ui_client",
    "start": "npm run watch:ui",
    "build": "npm run build:ui",
    "build-deploy": "npm run build && forge deploy"
  },
  "workspaces": [
    "static/ui_client"
  ],
  "devDependencies": {
    "prettier": "^3.3.3",
    "process": "^0.11.10"
  },
  "dependencies": {
    "@forge/api": "5.1.0",
    "@forge/resolver": "1.6.5"
  }
}
